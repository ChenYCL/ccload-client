{
  "source": "https://geminicli.com/docs/cli/settings/",
  "keys": [
    {
      "key": "advanced.autoConfigureMemory",
      "description": "Automatically configure Node.js memory limits. Note: Because memory is allocated during the initial process boot, this setting is only read from the global use…",
      "default": "true"
    },
    {
      "key": "advanced.ignoreLocalEnv",
      "description": "Whether to ignore generic .env files in the project directory.",
      "default": "false"
    },
    {
      "key": "agents.browser.blockFileUploads",
      "description": "Hard-block file upload requests from the browser agent.",
      "default": "false"
    },
    {
      "key": "agents.browser.confirmSensitiveActions",
      "description": "Require manual confirmation for sensitive browser actions (e.g., fill_form, evaluate_script).",
      "default": "false"
    },
    {
      "key": "billing.overageStrategy",
      "description": "How to handle quota exhaustion when AI credits are available. 'ask' prompts each time, 'always' automatically uses credits, 'never' disables credit usage.",
      "default": "ask"
    },
    {
      "key": "CODE_ASSIST_ENDPOINT",
      "description": "Code Assist backend endpoint.",
      "default": ""
    },
    {
      "key": "context.discoveryMaxDirs",
      "description": "Maximum number of directories to search for memory.",
      "default": "200"
    },
    {
      "key": "context.fileFiltering.enableFuzzySearch",
      "description": "Enable fuzzy search when searching for files.",
      "default": "true"
    },
    {
      "key": "context.fileFiltering.enableRecursiveFileSearch",
      "description": "Enable recursive file search functionality when completing @ references in the prompt.",
      "default": "true"
    },
    {
      "key": "context.fileFiltering.respectGeminiIgnore",
      "description": "Respect .geminiignore files when searching.",
      "default": "true"
    },
    {
      "key": "context.fileFiltering.respectGitIgnore",
      "description": "Respect .gitignore files when searching.",
      "default": "true"
    },
    {
      "key": "context.loadMemoryFromIncludeDirectories",
      "description": "Controls how /memory reload loads GEMINI.md files. When true, include directories are scanned; when false, only the current directory is used.",
      "default": "false"
    },
    {
      "key": "experimental.autoMemory",
      "description": "Automatically extract memory patches and skills from past sessions in the background. Every change is written as a unified diff `.patch` file under `<projectMe…",
      "default": "false"
    },
    {
      "key": "experimental.contextManagement",
      "description": "Enable logic for context management.",
      "default": "false"
    },
    {
      "key": "experimental.directWebFetch",
      "description": "Enable web fetch behavior that bypasses LLM summarization.",
      "default": "false"
    },
    {
      "key": "experimental.gemma",
      "description": "Enable access to Gemma 4 models via Gemini API.",
      "default": "true"
    },
    {
      "key": "experimental.gemmaModelRouter.autoStartServer",
      "description": "Automatically start the LiteRT-LM server when Gemini CLI starts and the Gemma router is enabled.",
      "default": "false"
    },
    {
      "key": "experimental.gemmaModelRouter.enabled",
      "description": "Enable the Gemma Model Router (experimental). Requires a local endpoint serving Gemma via the Gemini API using LiteRT-LM shim.",
      "default": "false"
    },
    {
      "key": "experimental.generalistProfile",
      "description": "Suitable for general coding and software development tasks.",
      "default": "false"
    },
    {
      "key": "experimental.modelSteering",
      "description": "Enable model steering (user hints) to guide the model during tool execution.",
      "default": "false"
    },
    {
      "key": "experimental.useOSC52Copy",
      "description": "Use OSC 52 for copying. This may be more robust than the default system when using remote terminal sessions (if your terminal is configured to allow it).",
      "default": "false"
    },
    {
      "key": "experimental.useOSC52Paste",
      "description": "Use OSC 52 for pasting. This may be more robust than the default system when using remote terminal sessions (if your terminal is configured to allow it).",
      "default": "false"
    },
    {
      "key": "experimental.voice.activationMode",
      "description": "How to trigger voice recording with the Space key.",
      "default": "push-to-talk"
    },
    {
      "key": "experimental.voice.backend",
      "description": "The backend to use for voice transcription. Note: When using the Gemini Live backend, voice recordings are sent to Google Cloud for transcription.",
      "default": "gemini-live"
    },
    {
      "key": "experimental.voice.stopGracePeriodMs",
      "description": "How long to wait for final transcription after stopping recording.",
      "default": "4000"
    },
    {
      "key": "experimental.voice.whisperModel",
      "description": "The Whisper model to use for local transcription.",
      "default": "ggml-base.en.bin"
    },
    {
      "key": "experimental.voiceMode",
      "description": "Enable experimental voice dictation and commands (/voice, /voice model).",
      "default": "false"
    },
    {
      "key": "experimental.worktrees",
      "description": "Enable automated Git worktree management for parallel work.",
      "default": "false"
    },
    {
      "key": "GEMINI_MODEL",
      "description": "Default Gemini model id (process env).",
      "default": ""
    },
    {
      "key": "GEMINI_SANDBOX",
      "description": "Sandbox mode for tool execution.",
      "default": ""
    },
    {
      "key": "GEMINI_SYSTEM_MD",
      "description": "Override path to the system prompt markdown.",
      "default": ""
    },
    {
      "key": "GEMINI_TELEMETRY_ENABLED",
      "description": "Enable or disable telemetry.",
      "default": ""
    },
    {
      "key": "GEMINI_TELEMETRY_LOG_PROMPTS",
      "description": "Whether telemetry logs prompts.",
      "default": ""
    },
    {
      "key": "GEMINI_TELEMETRY_OTLP_ENDPOINT",
      "description": "OTLP collector endpoint.",
      "default": ""
    },
    {
      "key": "GEMINI_TELEMETRY_OTLP_PROTOCOL",
      "description": "OTLP protocol (grpc or http).",
      "default": ""
    },
    {
      "key": "GEMINI_TELEMETRY_OUTFILE",
      "description": "Write telemetry to this file.",
      "default": ""
    },
    {
      "key": "GEMINI_TELEMETRY_TARGET",
      "description": "Telemetry target.",
      "default": ""
    },
    {
      "key": "GEMINI_TELEMETRY_TRACES_ENABLED",
      "description": "Enable telemetry traces.",
      "default": ""
    },
    {
      "key": "GEMINI_TELEMETRY_USE_COLLECTOR",
      "description": "Send telemetry via collector.",
      "default": ""
    },
    {
      "key": "GEMINI_WRITE_SYSTEM_MD",
      "description": "Write the effective system prompt to disk.",
      "default": ""
    },
    {
      "key": "general.debugKeystrokeLogging",
      "description": "Enable debug logging of keystrokes to the console.",
      "default": "false"
    },
    {
      "key": "general.defaultApprovalMode",
      "description": "The default approval mode for tool execution. 'default' prompts for approval, 'auto_edit' auto-approves edit tools, and 'plan' is read-only mode. YOLO mode (au…",
      "default": "default"
    },
    {
      "key": "general.enableAutoUpdate",
      "description": "Enable automatic updates.",
      "default": "true"
    },
    {
      "key": "general.enableNotifications",
      "description": "Enable terminal run-event notifications for action-required prompts and session completion.",
      "default": "false"
    },
    {
      "key": "general.logRagSnippets",
      "description": "Log full Code Customization (RAG) retrieved snippets to a local file for debugging.",
      "default": "false"
    },
    {
      "key": "general.maxAttempts",
      "description": "Maximum number of attempts for requests to the main chat model. Cannot exceed 10.",
      "default": "10"
    },
    {
      "key": "general.notificationMethod",
      "description": "How to send terminal notifications.",
      "default": "auto"
    },
    {
      "key": "general.plan.directory",
      "description": "The directory where planning artifacts are stored. If not specified, defaults to the system temporary directory. A custom directory requires a policy to allow …",
      "default": ""
    },
    {
      "key": "general.plan.enabled",
      "description": "Enable Plan Mode for read-only safety during planning.",
      "default": "true"
    },
    {
      "key": "general.plan.modelRouting",
      "description": "Automatically switch between Pro and Flash models based on Plan Mode status. Uses Pro for the planning phase and Flash for the implementation phase.",
      "default": "true"
    },
    {
      "key": "general.retryFetchErrors",
      "description": "Retry on \"exception TypeError: fetch failed sending request\" errors.",
      "default": "true"
    },
    {
      "key": "general.sessionRetention.enabled",
      "description": "Enable automatic session cleanup",
      "default": "true"
    },
    {
      "key": "general.sessionRetention.maxAge",
      "description": "Automatically delete chats older than this time period (e.g., \"30d\", \"7d\", \"24h\", \"1w\")",
      "default": "30d"
    },
    {
      "key": "general.topicUpdateNarration",
      "description": "Enable the Topic & Update communication model for reduced chattiness and structured progress reporting.",
      "default": "true"
    },
    {
      "key": "general.vimMode",
      "description": "Enable Vim keybindings",
      "default": "false"
    },
    {
      "key": "GOOGLE_APPLICATION_CREDENTIALS",
      "description": "Path to Google service-account JSON.",
      "default": ""
    },
    {
      "key": "GOOGLE_CLOUD_LOCATION",
      "description": "Vertex AI region.",
      "default": ""
    },
    {
      "key": "GOOGLE_CLOUD_PROJECT",
      "description": "Vertex AI / Google Cloud project.",
      "default": ""
    },
    {
      "key": "GOOGLE_GENAI_API_VERSION",
      "description": "Google GenAI API version.",
      "default": ""
    },
    {
      "key": "GOOGLE_VERTEX_BASE_URL",
      "description": "Override Vertex AI endpoint.",
      "default": ""
    },
    {
      "key": "hooksConfig.enabled",
      "description": "Canonical toggle for the hooks system. When disabled, no hooks will be executed.",
      "default": "true"
    },
    {
      "key": "hooksConfig.notifications",
      "description": "Show visual indicators when hooks are executing.",
      "default": "true"
    },
    {
      "key": "ide.enabled",
      "description": "Enable IDE integration mode.",
      "default": "false"
    },
    {
      "key": "model.compressionThreshold",
      "description": "The fraction of context usage at which to trigger context compression (e.g. 0.2, 0.3).",
      "default": "0.5"
    },
    {
      "key": "model.disableLoopDetection",
      "description": "Disable automatic detection and prevention of infinite loops.",
      "default": "false"
    },
    {
      "key": "model.maxSessionTurns",
      "description": "Maximum number of user/model/tool turns to keep in a session. -1 means unlimited.",
      "default": "-1"
    },
    {
      "key": "model.name",
      "description": "The Gemini model to use for conversations.",
      "default": ""
    },
    {
      "key": "model.skipNextSpeakerCheck",
      "description": "Skip the next speaker check.",
      "default": "true"
    },
    {
      "key": "output.format",
      "description": "The format of the CLI output. Can be `text` or `json`.",
      "default": "text"
    },
    {
      "key": "security.autoAddToPolicyByDefault",
      "description": "When enabled, the \"Allow for all future sessions\" option becomes the default choice for low-risk tools in trusted workspaces.",
      "default": "false"
    },
    {
      "key": "security.blockGitExtensions",
      "description": "Blocks installing and loading extensions from Git.",
      "default": "false"
    },
    {
      "key": "security.disableAlwaysAllow",
      "description": "Disable \"Always allow\" options in tool confirmation dialogs.",
      "default": "false"
    },
    {
      "key": "security.disableYoloMode",
      "description": "Disable YOLO mode, even if enabled by a flag.",
      "default": "false"
    },
    {
      "key": "security.enableConseca",
      "description": "Enable the context-aware security checker. This feature uses an LLM to dynamically generate and enforce security policies for tool use based on your prompt, pr…",
      "default": "false"
    },
    {
      "key": "security.enablePermanentToolApproval",
      "description": "Enable the \"Allow for all future sessions\" option in tool confirmation dialogs.",
      "default": "false"
    },
    {
      "key": "security.environmentVariableRedaction.enabled",
      "description": "Enable redaction of environment variables that may contain secrets.",
      "default": "false"
    },
    {
      "key": "security.toolSandboxing",
      "description": "Tool-level sandboxing. Isolates individual tools instead of the entire CLI process.",
      "default": "false"
    },
    {
      "key": "skills.enabled",
      "description": "Enable Agent Skills.",
      "default": "true"
    },
    {
      "key": "tools.disableLLMCorrection",
      "description": "Disable LLM-based error correction for edit tools. When enabled, tools will fail immediately if exact string matches are not found, instead of attempting to se…",
      "default": "true"
    },
    {
      "key": "tools.sandboxNetworkAccess",
      "description": "Whether the sandbox is allowed to access the network.",
      "default": "false"
    },
    {
      "key": "tools.shell.enableInteractiveShell",
      "description": "Use node-pty for an interactive shell experience. Fallback to child_process still applies.",
      "default": "true"
    },
    {
      "key": "tools.shell.showColor",
      "description": "Show color in shell output.",
      "default": "true"
    },
    {
      "key": "tools.truncateToolOutputThreshold",
      "description": "Maximum characters to show when truncating large tool outputs. Set to 0 or negative to disable truncation.",
      "default": "40000"
    },
    {
      "key": "tools.useRipgrep",
      "description": "Use ripgrep for file content search instead of the fallback implementation. Provides faster search performance.",
      "default": "true"
    },
    {
      "key": "ui.accessibility.screenReader",
      "description": "Render output in plain-text to be more screen reader accessible",
      "default": "false"
    },
    {
      "key": "ui.autoThemeSwitching",
      "description": "Automatically switch between default light and dark themes based on terminal background color.",
      "default": "true"
    },
    {
      "key": "ui.compactToolOutput",
      "description": "Display tool outputs (like directory listings and file reads) in a compact, structured format.",
      "default": "true"
    },
    {
      "key": "ui.dynamicWindowTitle",
      "description": "Update the terminal window title with current status icons (Ready: ◇, Action Required: ✋, Working: ✦)",
      "default": "true"
    },
    {
      "key": "ui.errorVerbosity",
      "description": "Controls whether recoverable errors are hidden (low) or fully shown (full).",
      "default": "low"
    },
    {
      "key": "ui.escapePastedAtSymbols",
      "description": "When enabled, @ symbols in pasted text are escaped to prevent unintended @path expansion.",
      "default": "false"
    },
    {
      "key": "ui.footer.hideContextPercentage",
      "description": "Hides the context window usage percentage.",
      "default": "true"
    },
    {
      "key": "ui.footer.hideCWD",
      "description": "Hide the current working directory in the footer.",
      "default": "false"
    },
    {
      "key": "ui.footer.hideModelInfo",
      "description": "Hide the model name and context usage in the footer.",
      "default": "false"
    },
    {
      "key": "ui.footer.hideSandboxStatus",
      "description": "Hide the sandbox status indicator in the footer.",
      "default": "false"
    },
    {
      "key": "ui.hideBanner",
      "description": "Hide the application banner",
      "default": "false"
    },
    {
      "key": "ui.hideContextSummary",
      "description": "Hide the context summary (GEMINI.md, MCP servers) above the input.",
      "default": "false"
    },
    {
      "key": "ui.hideFooter",
      "description": "Hide the footer from the UI",
      "default": "false"
    },
    {
      "key": "ui.hideTips",
      "description": "Hide helpful tips in the UI",
      "default": "false"
    },
    {
      "key": "ui.hideWindowTitle",
      "description": "Hide the window title bar",
      "default": "false"
    },
    {
      "key": "ui.incrementalRendering",
      "description": "Enable incremental rendering for the UI. This option will reduce flickering but may cause rendering artifacts. Only supported when useAlternateBuffer is enable…",
      "default": "true"
    },
    {
      "key": "ui.inlineThinkingMode",
      "description": "Display model thinking inline: off or full.",
      "default": "off"
    },
    {
      "key": "ui.loadingPhrases",
      "description": "What to show while the model is working: tips, witty comments, all, or off.",
      "default": "off"
    },
    {
      "key": "ui.renderProcess",
      "description": "Enable Ink render process for the UI.",
      "default": "true"
    },
    {
      "key": "ui.showCitations",
      "description": "Show citations for generated text in the chat.",
      "default": "false"
    },
    {
      "key": "ui.showCompatibilityWarnings",
      "description": "Show warnings about terminal or OS compatibility issues.",
      "default": "true"
    },
    {
      "key": "ui.showHomeDirectoryWarning",
      "description": "Show a warning when running Gemini CLI in the home directory.",
      "default": "true"
    },
    {
      "key": "ui.showLineNumbers",
      "description": "Show line numbers in the chat.",
      "default": "true"
    },
    {
      "key": "ui.showMemoryUsage",
      "description": "Display memory usage information in the UI",
      "default": "false"
    },
    {
      "key": "ui.showModelInfoInChat",
      "description": "Show the model name in the chat for each model turn.",
      "default": "false"
    },
    {
      "key": "ui.showShortcutsHint",
      "description": "Show the \"? for shortcuts\" hint above the input.",
      "default": "true"
    },
    {
      "key": "ui.showSpinner",
      "description": "Show the spinner during operations.",
      "default": "true"
    },
    {
      "key": "ui.showStatusInTitle",
      "description": "Show Gemini CLI model thoughts in the terminal window title during the working phase",
      "default": "false"
    },
    {
      "key": "ui.showUserIdentity",
      "description": "Show the signed-in user's identity (e.g. email) in the UI.",
      "default": "true"
    },
    {
      "key": "ui.terminalBackgroundPollingInterval",
      "description": "Interval in seconds to poll the terminal background color.",
      "default": "60"
    },
    {
      "key": "ui.terminalBuffer",
      "description": "Use the new terminal buffer architecture for rendering.",
      "default": "false"
    },
    {
      "key": "ui.useAlternateBuffer",
      "description": "Use an alternate screen buffer for the UI, preserving shell history.",
      "default": "false"
    },
    {
      "key": "ui.useBackgroundColor",
      "description": "Whether to use background colors in the UI.",
      "default": "true"
    }
  ]
}
